# slide-workspace

This is the agent workspace for creating slides in HTML format.

## Overview

- **Purpose**: Create and deploy presentation slides as HTML files
- **Skills**: Available in the `skills/` directory
- **Deployment**: See `DEPLOY.md` for deployment instructions

## Available CLI

- `wrangler` — Cloudflare Workers CLI, used for deployment
